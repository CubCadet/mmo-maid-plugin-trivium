# Development dependencies — NOT shipped in the upload zip.
# These power local dev loops, tests, and CI.

pytest>=7.0
pyyaml>=6.0.3           # robust events.yaml parsing for `mmo dev`
mmo-maid-sdk>=0.5.2,<0.6.0   # MockContext, make_event, the `mmo` CLI
