# Development dependencies — NOT shipped in the upload zip.
# These power local dev loops, tests, and CI.

pytest>=7.0
pyyaml>=6.0           # robust events.yaml parsing for `yourbot dev`
yourbot-sdk>=0.7.0,<0.8.0   # MockContext, make_event, MockClock, the `yourbot` CLI
