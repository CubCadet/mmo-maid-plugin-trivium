# Development dependencies — NOT shipped in the upload zip.
# These power local dev loops, tests, and CI.

pytest>=7.0
pyyaml>=6.0           # robust events.yaml parsing for `mmo dev`
mmo-maid-sdk>=0.6.0,<0.7.0   # MockContext, make_event, MockClock, the `mmo` CLI
